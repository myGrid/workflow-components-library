- if user_signed_in?
  %li
    = link_to('Sign out', destroy_user_session_path)
- else
  %li
    = link_to('Sign in', new_user_session_path)
%li
  User:
  - if current_user
    = current_user.name
  - else
    (not logged in)
