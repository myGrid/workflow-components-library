= semantic_form_for @component do |f|
  = f.inputs do
    = f.error_messages
    = f.input :label, :hint => "A short title that will be seen in most places. Example: 'DNA 2 RNA'"
    = f.input :title
    = f.input :taverna_activity
    = f.input :description
    = f.buttons do
      = f.commit_button
