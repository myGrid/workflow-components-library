%nav#main_nav
  #site_tabs
    %ul
      %li{:class => "#{@selected_site_tab == :home ? 'active' : ''}" }= link_to 'Home', root_path
      %li{:class => "#{@selected_site_tab == :components ? 'active' : ''}" }= link_to 'Components', components_path
      %li{:class => "#{@selected_site_tab == :new_component ? 'active' : ''}" }= link_to 'New Component', new_component_path